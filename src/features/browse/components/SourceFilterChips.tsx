import { cn } from "@/lib/utils";
const sources = ["System", "Managed", "Google", "Fontsource"];
export function SourceFilterChips({ active, onChange }: { active: string[]; onChange: (sources: string[]) => void }) { return <div className="flex flex-wrap gap-2">{sources.map((source) => <button key={source} className={cn("rounded-md border px-3 py-1.5 text-xs", active.includes(source) ? "border-border bg-selected text-foreground" : "border-border bg-surface text-muted-foreground")} type="button" onClick={() => onChange(active.includes(source) ? active.filter((item) => item !== source) : [...active, source])}>{source}</button>)}</div>; }
