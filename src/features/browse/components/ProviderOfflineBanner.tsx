export function ProviderOfflineBanner({ provider, cached }: { provider: string; cached: boolean }) { return <div className="rounded-md border bg-muted px-3 py-2 text-xs text-foreground">{provider} unavailable. {cached ? "Showing cached fonts." : "Browse remains available from other sources."}</div>; }
