export function ProviderOfflineBanner({ provider, cached }: { provider: string; cached: boolean }) { return <div className="rounded-xl border border-orange-300/20 bg-orange-400/10 px-3 py-2 text-xs text-orange-100">{provider} unavailable. {cached ? "Showing cached fonts." : "Browse remains available from other sources."}</div>; }
